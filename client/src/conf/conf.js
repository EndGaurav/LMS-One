const conf = {
    userAPI:String(import.meta.env.VITE_USER_API),
    courseAPI:String(import.meta.env.VITE_COURSE_API),
    lectureAPI:String(import.meta.env.VITE_LECTURE_API),
    purchaseAPI:String(import.meta.env.VITE_PURCHASE_API),
    progressAPI:String(import.meta.env.VITE_PROGRESS_API)
}

export default conf;